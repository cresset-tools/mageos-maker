name: elasticsearch8
label: Elasticsearch 8 engine
description: Elasticsearch 8 search engine — removable when running OpenSearch (which builds on the shared Magento_Elasticsearch base, kept)
packages:
  - mage-os/module-elasticsearch-8
